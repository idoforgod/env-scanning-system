# Source Evolver Agent

소스 Tier 자동 승격/강등 및 안전장치 적용을 담당하는 에이전트.

## 역할

1. **성과 기반 Tier 조정**: pSRT 점수와 스캔 이력을 기반으로 소스 Tier 자동 조정
2. **안전장치 적용**: 변경 상한선, 화이트리스트/블랙리스트, 이상 탐지
3. **스냅샷 관리**: 변경 전 스냅샷 저장 및 롤백 지원
4. **변경 이력 기록**: 모든 변경을 evolution-log.json에 기록

## 입력 파일

- `config/source-performance.json`: 소스별 성과 데이터
- `config/evolution-config.json`: 진화 규칙 정의
- `config/evolution-whitelist.json`: 보호 소스 목록
- `config/evolution-blacklist.json`: 차단 도메인 목록
- `config/regular-sources.json`: 현재 소스 목록

## 출력 파일

- `config/regular-sources.json`: 업데이트된 소스 목록
- `evolution/evolution-log.json`: 변경 이력
- `evolution/snapshots/{year}/{month}/`: 스냅샷

## 실행 로직

```
┌─────────────────────────────────────────────────────────────────────┐
│                    Source Evolver 실행 흐름                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  1. 스냅샷 생성                                                      │
│     └─ 현재 config 상태 저장 (롤백용)                                │
│                                                                     │
│  2. 성과 데이터 로드                                                 │
│     └─ source-performance.json에서 각 소스별 pSRT, trend 확인       │
│                                                                     │
│  3. 승격 후보 식별                                                   │
│     └─ pSRT ≥ 70 AND scans ≥ 3 AND trend = "improving"              │
│     └─ whitelist 확인 (min_tier 이상으로만 승격)                     │
│                                                                     │
│  4. 강등 후보 식별                                                   │
│     └─ pSRT ≤ 40 AND scans ≥ 3 AND trend = "declining"              │
│     └─ whitelist 확인 (min_tier 아래로 강등 불가)                    │
│                                                                     │
│  5. 비활성화 후보 식별                                               │
│     └─ pSRT ≤ 30 AND scans ≥ 5 AND signals_found = 0                │
│     └─ whitelist 확인 (보호 소스는 비활성화 불가)                    │
│     └─ STEEPS 균형 확인 (최소 소스 수 유지)                          │
│                                                                     │
│  6. 상한선 적용                                                      │
│     └─ 최대 변경 수 초과 시 pending으로 이동                         │
│                                                                     │
│  7. 이상 탐지                                                        │
│     └─ 급격한 변화 감지 시 pause_and_alert                           │
│                                                                     │
│  8. 변경 적용                                                        │
│     └─ regular-sources.json 업데이트                                 │
│     └─ evolution-log.json에 기록                                     │
│                                                                     │
│  9. 결과 리포트                                                      │
│     └─ 변경 요약 출력                                                │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

## 승격 규칙 (Promotion)

```json
{
  "conditions": {
    "min_pSRT_avg": 70,
    "min_scans": 3,
    "required_trend": "improving",
    "min_signals_found": 2
  },
  "action": {
    "tier_change": -1,
    "max_tier": 1
  }
}
```

**예시:**
- Bloomberg: pSRT 60.7 → 70+ 달성 시 Tier 2 → Tier 1 승격
- 단, whitelist에서 min_tier가 2인 경우 Tier 1로 승격 불가

## 강등 규칙 (Demotion)

```json
{
  "conditions": {
    "max_pSRT_avg": 40,
    "min_scans": 3,
    "required_trend": "declining"
  },
  "action": {
    "tier_change": 1,
    "min_tier": 4
  }
}
```

**예시:**
- FOX 13: pSRT 34.0 → Tier 3 → Tier 4 강등
- whitelist 소스는 min_tier 아래로 강등 불가

## 비활성화 규칙 (Deactivation)

```json
{
  "conditions": {
    "max_pSRT_avg": 30,
    "min_scans": 5,
    "max_signals_found": 0
  },
  "action": {
    "status": "inactive"
  }
}
```

**주의:**
- whitelist 소스는 비활성화 불가
- STEEPS 카테고리별 최소 소스 수 유지 필수

## 안전장치

### 1. 변경 상한선
```
1회 실행당 최대 변경:
- 소스 Tier 변경: 5개
- 소스 비활성화: 3개
```

### 2. 화이트리스트 보호
- `evolution-whitelist.json`의 소스는 규칙에서 제외
- absolute: 강등/비활성화 완전 불가
- conditional: min_tier 아래로 강등 불가

### 3. 이상 탐지
```
- pSRT 급락 (30% 이상): pause_and_alert
- 신호 수 급감 (50% 이상): pause_and_alert
- 대량 강등 (10개 이상): pause_and_alert
```

### 4. 스냅샷 & 롤백
- 모든 변경 전 스냅샷 자동 저장
- `/evolution:rollback 2026-01-12` 명령으로 복원

## 호출 방법

```
# Marathon Mode 완료 후 자동 호출
@source-evolver

# 수동 호출
/evolution:apply
```

## 출력 예시

```
┌─────────────────────────────────────────────────────────────────────┐
│                    Source Evolution Report                          │
│                    2026-01-12                                        │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ✅ Promotions (2)                                                  │
│     • Bloomberg: Tier 2 → Tier 1 (pSRT: 72.3, trend: improving)    │
│     • Crunchbase: Tier 2 → Tier 1 (pSRT: 71.5, trend: improving)   │
│                                                                     │
│  ⬇️ Demotions (1)                                                  │
│     • FOX 13: Tier 3 → Tier 4 (pSRT: 34.0, trend: declining)       │
│                                                                     │
│  🔴 Deactivations (0)                                               │
│     (none)                                                          │
│                                                                     │
│  ⏸️ Pending (overflow)                                              │
│     • 3 changes queued for next cycle                               │
│                                                                     │
│  📊 Statistics                                                      │
│     • Total sources: 177 → 177                                      │
│     • Tier 1: 50 → 52                                               │
│     • Tier 2: 65 → 63                                               │
│     • Tier 3: 42 → 42                                               │
│     • Tier 4: 20 → 20                                               │
│                                                                     │
│  🔒 Snapshot saved: snapshots/2026/01/2026-01-12-pre-evolution.json │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

## 관련 슬래시 커맨드

| 커맨드 | 설명 |
|--------|------|
| `/evolution:apply` | 수동으로 진화 규칙 적용 |
| `/evolution:status` | 현재 진화 상태 확인 |
| `/evolution:rollback [date]` | 지정 날짜 스냅샷으로 복원 |
| `/evolution:pending` | 대기 중인 변경 확인 |
| `/evolution:whitelist add [domain]` | 화이트리스트에 추가 |
| `/evolution:blacklist add [domain]` | 블랙리스트에 추가 |
